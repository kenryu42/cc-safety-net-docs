---
title: "セキュリティモデル"
sidebarTitle: "セキュリティモデル"
description: "CC Safety Net の信頼モデルを説明します。AI からシェルへの境界、各安全レベルの保証、設定復旧の境界、脆弱性の開示分類を扱います。"
---

CC Safety Net は、AI コーディングエージェントのような信頼できないコマンドの発生源と、実際の実行環境との間に位置します。このページでは、信頼境界、各安全レベルの保証、設定が壊れたときの扱い、シークレット保護、攻撃対象領域を示します。脆弱性の報告方法は[セキュリティポリシー](/ja/security)を参照してください。

CC Safety Net は、対応するコーディングエージェントのツール呼び出しに対する、ベストエフォートの静的な実行前ポリシーゲートです。OS のサンドボックスでも権限境界でもなく、インストール済みの連携を迂回するコマンドに対する保護でもありません。

## 信頼境界

### 主境界：コマンドの発生源と実行環境の間

中心となる信頼境界は、AI コーディングエージェントとホストのシェルの間にあります。CC Safety Net は、この境界でコマンドを検査します。

- **信頼できない側**：AI エージェントが生成するコマンド文字列。プロンプトインジェクション、混乱したコンテキスト、敵対的な指示によって破壊的なコマンドを生成しうるため、潜在的に敵対的なものとして扱います。
- **実行する側**：コマンドを実行するホストのシェル。

対応プラットフォームのシェルツールに届くコマンドは、実行が許可される前に必ず解析エンジンを通ります。解析がブロックの理由を返した場合、そのコマンドは拒否されます。

この境界は、対応するツール名と形式の範囲で終わります。アダプターがコマンド実行として扱うのは、連携ごとに定めた特定のツール名だけです。未知のツールも、ポリシーファイル、Git メタデータ、機密パスについては保守的に検査されますが、その内容がシェルコマンドとして扱われることはありません。インストール済みの連携を完全に迂回するコマンドは、この境界の外側です。

### 副次的な境界

副次的な境界が 4 つあり、いずれも外部から CC Safety Net の内側へ入ってきます。どの入力も、解析に影響を与える前に検証されます。

| 境界 | 入力元 | 検証方法 |
| --- | --- | --- |
| ユーザー設定 | ディスク上の `policy.json` とルール設定 | パースとスキーマ検証を行う。拒否された候補は適用されず、ランタイムは通常の作業を拒否せずにフォールバックする（下記の[設定復旧の境界](#configuration-recovery-boundary)を参照） |
| rulebook の設定元 | GitHub またはローカルディレクトリから取得する rulebook | リモートの rulebook はロックファイルの SHA-256 ダイジェストで完全性を検証する。検証できない設定元からはルールを読み込まない |
| hook の入力 JSON | 各エージェントが標準入力に送る JSON ペイロード | 防御的にパースする。不正または過大な入力は拒否になる |
| 環境変数 | レベルと機能のフラグ、パスの上書き（`CC_SAFETY_NET_*`、`TMPDIR` など） | 明示的に読み取る。セキュリティ上重要な値は信頼しない |

## 各安全レベルの保証

standard、strict、paranoid は、`fail_closed`、`paranoid_rm`、`paranoid_interpreters` の 3 つの機能に既定値を与える preset です。保証の内容はレベルごとに異なります。

| レベル | 保証の内容 |
| --- | --- |
| [standard](/ja/configuration/modes#既定モード) | 認識できる破壊的コマンドに対する**ベストエフォート**の保護。動的な実行ファイル、コマンド置換で組み立てられたコマンド構造、検証できない再帰削除の対象、組み込みの機密パスに対する単独のメタデータのみのチェック、完全にリテラルなローカルの生成コマンド 1 つに対する `eval`／`source` は、意図的に許可する。パースできなくても安全に見えるテキストは通し、破壊的に見えるテキストは保守的なヒューリスティックで検出する |
| [strict](/ja/configuration/modes#strict-mode) | fail closed の機能を追加する。パースできない入力は通さずに拒否し、`rm -rf "$target"` のような検証できない破壊的対象と、メタデータのみによる機密パスの探索をブロックする |
| [paranoid](/ja/configuration/modes#paranoid-mode) | strict にさらに 2 つの制限を加える。カレントディレクトリ配下であっても一時パス以外への[再帰的な強制削除](/ja/configuration/modes#paranoid-rm-check)をブロックし、内容にかかわらず[すべてのインタープリター 1 行コード](/ja/configuration/modes#paranoid-interpreters)をブロックする |

<Warning>
  standard モードは**敵対的な入力に耐える水準ではありません**。動的な `rm -rf` の対象は、standard では一律にはブロックされません。`rm -rf "$target"` は standard では許可され、ブロックされるのは strict と paranoid だけです。コマンドがプロンプトインジェクションなど敵対的な文脈から来る可能性がある場合は、strict または paranoid が必要です。
</Warning>

シークレット保護が有効な間は、安全レベルによって、一致した機密情報の**内容**へのアクセスや、ユーザーが設定した deny path とその配下が緩和されることはありません。設定した `secret_protection.allow_paths` の項目により、特定のファイルやディレクトリツリーを CLI 以外の組み込みシークレットルールから除外できますが、deny path と `secret.cli.*` のルールは引き続き優先されます。致命的な操作に対する保護は常に適用されます。これには、root またはユーザーのホームディレクトリの再帰削除、保護対象の Git メタデータへの破壊的な変更、正規のユーザー `policy.json` への破壊的な変更が含まれます。

<span id="configuration-recovery-boundary"></span>

## 設定復旧の境界

設定は信頼境界であって、緊急停止スイッチではありません。設定が無効な場合、ランタイムは 2 つの状態のいずれかになりますが、**設定が無効だというだけの理由で通常の作業を拒否することはありません**。

- **`ready`**：有効なすべての設定元が検証済みです。
- **`degraded`**：候補となった設定元が拒否され、代わりに安全なものが適用されています。検証できないルールの設定元は破棄され、ルールを提供しません。内容がずれている、または無効なローカル rulebook については、ダイジェスト検証済みのキャッシュがそのまま使われます。rulebook 名が重複した場合は、先に名前を確保した側が残ります。読み取れないポリシーファイルは、救済されたポリシーか、組み込みの安全側の既定値にフォールバックします。

拒否された候補が有効なものとして扱われることはありません。設定元を破棄すると、その設定元が提供していた拒否も失われます。これは、設定したポリシーと比べて実際に強制力が下がることを意味するため、安全性に影響しないものとは扱わず、すべての表示箇所で報告します。ただし、破棄によって組み込みのルールが弱まることはありません。rulebook が提供できるのはブロックのルールだけであり、読み取れない `rule.json` を無視すれば、その `overrides` が無効にしていた組み込みルールはむしろ復活します。例外は 1 つだけで、範囲も文書化されています。`transparent_wrappers` は `rule.json` の中にあるため、`rule.json` を読み取れない場合、そのスコープでは組み込みの解析が透過できるラップ済みコマンドが減ります。設定できないことを理由に何かを拒否しない代わりに、コマンドやパスを許可リストに追加することもありません。

ポリシーファイルの保護と Git メタデータの保護は、設定スナップショットの読み込み**より前**に評価されるため、どちらの状態でも同じように適用され、設定に関する情報を持ちません。

失敗の種類ごとの一覧、それによって生じるフォールバック、復旧用のコマンドを含む完全な取り決めは、[設定の復旧](/ja/configuration/recovery)にあります。

## fail closed による強制

fail closed は、解析そのものを完了できない場合に**その 1 回のツール呼び出し**へ適用されます。アナライザーの予期しない失敗、パースできない入力、リソース上限への到達が該当します。設定が無効なときの動作を指す言葉ではありません。

<Steps>
  <Step title="hook のエントリーポイント">
    hook のアダプターは、解析の呼び出しを try/catch で囲みます。解析が例外を投げた場合、コマンドを続行させず、「failed closed」の理由を添えた拒否を出力します。これは標準入力方式の hook を使うすべてのエージェント（Antigravity CLI、Claude Code、Cursor、Gemini CLI、GitHub Copilot CLI、Kimi Code）に当てはまります。
  </Step>
  <Step title="プラグインと拡張機能のエントリーポイント">
    Amp Code、OpenCode、OpenClaw、Pi のプロセス内連携も、同じパターンを使います。解析エラーを捕捉し、それをブロックメッセージとして返すため、各プラットフォームは拒否されたコマンドとして扱います。Codex はプラグインとしてインストールされますが、実行するのは共通の標準入力 hook のエントリーポイントなので、前のステップに含まれます。Hermes Agent は両方を重ねた構成です。管理対象の Python プラグインが同じ標準入力 hook（`cc-safety-net hook --hermes-agent`）を呼び出し、解析を完了できない場合はプラグイン自身がその呼び出しをブロックします。`npx` が見つからない、作業ディレクトリや Hermes のセッション記録を解決できない、プロセスの起動に失敗する、30 秒でタイムアウトする、アナライザーが 0 以外で終了する、出力を読み取れない、といった場合が対象です。各エージェントの方式は[連携アーキテクチャ](/ja/guides/integration-architecture)を参照してください。
  </Step>
  <Step title="形式が不正、またはサイズが大きすぎるツール入力">
    信頼できない入れ子構造のツール入力には、オブジェクトの深さ 64 段、走査する値 10,000 個、自身のキー 10,000 個、文字列 1 つあたり 1 MiB、文字列データの合計 4 MiB という上限を設けています。hook の標準入力は生のバイト数で 8 MiB が上限です。いずれかを超えた時点で、その呼び出しを拒否します。
  </Step>
  <Step title="パーサーのリソース枯渇">
    131,072 UTF-16 コードユニットを超える入力、16,384 を超える語数、64 段を超えるネストは、中途半端に解析せずに拒否します。これとは別に、派生トークン 16,384 個という上限があり、最初のパースの後にネストされたコマンドや埋め込まれたコマンドが追加する処理量を制限します。[パーサーと実行時の依存関係](/ja/guides/architecture#parsers-and-the-runtime-dependency-surface)を参照してください。いずれの上限も、standard を含む**すべての**安全レベルに適用されます。
  </Step>
  <Step title="strict モードでの動作">
    [strict モード](/ja/configuration/modes#strict-mode)は、シェルのパーサーが安全にトークン化できないコマンドにまで fail closed を広げます。そのため、パースできない入力は通さずにブロックします。standard モードでは、安全に見えるパース不能なテキストは許可されます。
  </Step>
</Steps>

<Note>
  設定が無効な場合は、意図的にこの一覧に含めていません。拒否されたルールの設定元は破棄され、読み取れないポリシーファイルは安全側の既定値にフォールバックするため、通常の作業はそのまま続けられます。上記の[設定復旧の境界](#configuration-recovery-boundary)を参照してください。
</Note>

この方針の理由は、[設計原則](/ja/guides/design-principles#解析を完了できない場合は-fail-closed)を参照してください。

## シークレットのマスク

コマンドやセグメントのテキストは、監査ログに書き込まれる前、またはエージェントに返される前に、自動的なシークレットのマスク処理を通ります。マスク対象は、PEM 形式の秘密鍵、データベース URL の環境変数、シークレットを含みやすい環境変数への代入、シークレットを含みやすい HTTP ヘッダー、URL 内の資格情報、署名付き URL のクエリパラメーター（`x-amz-signature`、`x-goog-signature`、`sig`、`signature`）、既知のプロバイダーのトークン接頭辞（GitHub、Slack、npm、Stripe、PyPI）、JWT、AWS のアクセスキー ID です。一致した値は `<redacted>` に置き換えられます。

このマスク処理は、パターンに基づく保守的な仕組みです。コマンドの引数に現れたシークレットが漏れるリスクは下げられますが、**対象は認識済みの資格情報の形式に限られます**。絶対パス、プロジェクト名やディレクトリ名、ホスト名、IP アドレス、ユーザー名、パターン一覧にない形式の資格情報は、そのまま残ります。新しいシークレットの形式は次々に登場するため、エージェントが実行するコマンドに本物の資格情報を渡さないでください。マスク処理の対象範囲は[監査ログのリファレンス](/ja/reference/audit-log#シークレットのマスク)にまとめてあります。

同じ制限は `cc-safety-net explain` にも当てはまります。トレースには、指定したコマンド文字列、解析後のトークン、ホームディレクトリを含む絶対パスがそのまま残ります。どこかに貼り付ける前に、必ず内容を確認してください。[explain トレース](/ja/reference/explain-trace)を参照してください。

## 攻撃対象領域

脅威モデルで想定する主な攻撃対象領域と、その緩和策は次のとおりです。

| 攻撃対象領域 | 攻撃者の狙い | 緩和策 |
| --- | --- | --- |
| **シェルコマンドのパーサー** | 通常でないクオート、入れ子のコマンド置換、演算子の曖昧さといったパーサーの境界事例を突くコマンド文字列を作り、破壊的な処理を隠す | 引用符が閉じていない場合のガードは、生の文字列を 1 つのセグメントとして返す。動的なコマンド置換を検出できるよう、変数参照は展開せずに保持する。strict モードはパースできないコマンドをブロックし、パーサーのエラーは fail closed になる |
| **ラッパーとインタープリターの除去** | `sudo`、`env`、`bash -c`、インタープリターの 1 行コードの背後に破壊的コマンドを隠す | ラッパーを繰り返し取り除く（回数に上限あり）。シェルラッパーとインタープリターのコードは、最大 10 階層まで再帰的に解析し直す。`transparent_wrappers` で宣言したコマンドは、解析前に見える子コマンドまで透過する |
| **機密ファイルへのアクセス** | コマンド、パス、検索、patch の各形式を使って、`.env`、`~/.ssh/id_*`、`~/.aws/credentials` などの資格情報を読み取る、または存在を突き止める | 機密パスの保護は、対応するコマンド、パス、検索、patch の各形式と、未知のツールに対するフォールバック検査を対象とする。ユーザーが設定した deny path とその配下が最初に一致し、緩和されることはない。シークレットの allow path は CLI 以外の組み込みルールの一致を除外できるが、コーディング CLI のパスは引き続き保護される。strict と paranoid では、メタデータのみによる探索もブロックする。汎用の読み取り境界ではなく、範囲の限られたパターン群である |
| **rm 解析でのパストラバーサル** | シンボリックリンクやパスの小細工で、危険な `rm -rf` の対象を分類から逃れさせる | 対象を正規化したパスに解決する。既知の一時ディレクトリの外を指す `$TMPDIR` の上書きを検出する。ただし TOCTOU の隙は残る（[既知の制限](/ja/guides/known-limitations#symlink-toctou-risk)を参照） |
| **rulebook のサプライチェーン** | GitHub の設定元から悪意ある rulebook を配布する | リモートの rulebook は、ロックファイルに対する SHA-256 検証とスキーマ検証を受ける。悪意ある rulebook はルールを追加できても、組み込みのブロックを取り除くことはできない |
| **監査ログへのシークレット混入** | ディスク上の監査ログにシークレットを書き込ませる | ログ書き込みの前に `redactSecrets` を実行する。パターン一覧は継続的に整備している |
| **hook 入力のパース** | 不正な JSON で hook をクラッシュさせる | `JSON.parse` の失敗はクラッシュではなく拒否になる。各プラットフォームのアダプターが追加の検証を行う |
| **監査ログのパストラバーサル** | ログディレクトリの外に書き込ませるセッション ID を作る | セッション ID をファイルシステムで安全な形に正規化し、長さの上限を設け、`.` と `..` を拒否する |

ネットワークレベルの攻撃と、エージェントのプラットフォーム自体に対する攻撃は対象外です。CC Safety Net はコマンド解析中にネットワーク通信を行わず、ネットワーク層を持ちません。リソース枯渇は封じ込めではなく上限で対処します。パーサーやツール入力の上限を超える入力は、中途半端に解析せずに拒否します。

## 開示の分類

報告手順の全体は[セキュリティポリシー](/ja/security)にあります。どちらの経路で報告すべきかは、次の表で判断してください。

| 分類 | 例 | 報告先 |
| --- | --- | --- |
| **バグ**：CC Safety Net が破壊的コマンドを止められなかった | 未対応のコマンド（ルールがまだブロックしない形式）、パーサー・トークナイザー・ラッパー解析の境界事例、コマンドを通してしまう解析エラー、安全なコマンドをブロックする誤検知 | 公開の GitHub Issue |
| **脆弱性**：CC Safety Net 自身が、本来しないはずの有害な動作をした | ブロックメッセージ、監査ログ、診断出力、デバッグ出力、誤検知レポートの入力補完からのシークレット漏えい（マスク処理の回避を含む）。細工された入力によって意図したディレクトリの外に書き込まれる、監査ログ記録や設定処理におけるパストラバーサルやファイルシステムの問題。rulebook の完全性を含む、公開 npm パッケージやプラグイン配布のサプライチェーン・パッケージングの問題 | 非公開での開示 |

未対応のコマンドを報告する場合は、コマンドの形式だけを示してください。そのまま貼り付けて悪用できるプロンプトインジェクションの攻撃コードは含めないでください。どちらの報告経路についても、[セキュリティポリシー](/ja/security)を参照してください。

## 関連ページ

- [セキュリティポリシー](/ja/security)：バグや脆弱性の報告方法。
- [設定の復旧](/ja/configuration/recovery)：`ready` と `degraded` の完全な取り決め。
- [設計原則](/ja/guides/design-principles)：fail closed と意味解析を選んだ理由。
- [既知の制限](/ja/guides/known-limitations)：シンボリックリンクの TOCTOU など、残るリスク。
- [監査ログ](/ja/reference/audit-log)：マスク済みのコマンド記録が書き込まれる場所。
