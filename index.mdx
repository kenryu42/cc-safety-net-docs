---
title: "CC Safety Net: Stop AI Agents from Destroying Your Work"
sidebarTitle: "Home"
description: "CC Safety Net blocks destructive git and filesystem commands before AI agents can execute them. Protect your work across Claude Code, Codex, Gemini CLI, GitHub Copilot CLI, Kimi Code, OpenCode, and Pi."
mode: "wide"
---

<div className="cc-home">
  <section className="cc-home-hero">
    <div className="cc-home-hero-copy">
      <p className="cc-home-kicker">Hard command safety for coding agents</p>
      <h1>Stop destructive shell commands before they run.</h1>
      <p className="cc-home-lede">
        CC Safety Net intercepts Bash calls from AI coding agents, analyzes destructive git and filesystem intent, and blocks operations that would discard local work.
      </p>
      <div className="cc-home-actions">
        <a className="cc-home-button cc-home-button-primary" href="/installation">Install for your agent</a>
        <a className="cc-home-button cc-home-button-secondary" href="/quickstart">Run the quickstart</a>
      </div>
    </div>

    <div className="cc-terminal-proof" aria-label="Example CC Safety Net command decision">
      <div className="cc-terminal-proof-header">
        <span>pretooluse / bash</span>
        <span>semantic command analysis</span>
      </div>
      <pre><code>{`$ git checkout -- .
CC Safety Net: BLOCKED
Reason: discards uncommitted changes permanently.

$ git checkout -b safety-check
CC Safety Net: ALLOWED
Reason: creates a branch without deleting work.`}</code></pre>
    </div>
  </section>

  <section className="cc-quickstart-strip" aria-label="Quickstart path">
    <div>
      <h2>Install, verify, see a block.</h2>
      <p>Start with your agent's install path, run `doctor`, then trigger one safe test block.</p>
    </div>
    <div className="cc-quickstart-links">
      <a href="/installation">Installation</a>
      <a href="/quickstart">Quickstart</a>
      <a href="/reference/cli-commands#doctor">Doctor reference</a>
    </div>
  </section>

  <section className="cc-model-section">
    <div className="cc-section-heading">
      <h2>Try the protection model</h2>
      <p>CC Safety Net looks at what the command will do, not only which words it contains.</p>
    </div>

    <Tabs>
      <Tab title="Destructive git">
        ```bash
        git checkout -- src/main.py
        ```

        <Warning>
          Blocked: this discards uncommitted changes in `src/main.py`. Stash or commit first.
        </Warning>
      </Tab>
      <Tab title="Shell wrappers">
        ```bash
        bash -c "git reset --hard"
        ```

        <Warning>
          Blocked: wrapped shell commands are recursively unwrapped before analysis.
        </Warning>
      </Tab>
      <Tab title="Project cleanup">
        ```bash
        rm -rf ./node_modules
        ```

        <Check>
          Allowed: the target is scoped inside the current project directory.
        </Check>
      </Tab>
    </Tabs>
  </section>

  <section className="cc-entry-section">
    <div className="cc-section-heading">
      <h2>Choose the next document</h2>
      <p>Use the docs by workflow: install first, tune protection second, inspect behavior when you need detail.</p>
    </div>

    <div className="cc-entry-grid">
      <a className="cc-entry-card cc-entry-card-primary" href="/installation">
        <span className="cc-entry-label">Get started</span>
        <span className="cc-entry-title">Install CC Safety Net</span>
        <span className="cc-entry-desc">Agent-specific setup for Claude Code, Codex, Gemini CLI, Copilot CLI, Kimi Code, OpenCode, and Pi.</span>
      </a>
      <a className="cc-entry-card" href="/configuration/modes">
        <span className="cc-entry-label">Configuration</span>
        <span className="cc-entry-title">Set protection modes</span>
        <span className="cc-entry-desc">Choose default, strict, paranoid, or worktree behavior for your workflow.</span>
      </a>
      <a className="cc-entry-card" href="/reference/blocked-commands">
        <span className="cc-entry-label">Reference</span>
        <span className="cc-entry-title">Inspect blocked commands</span>
        <span className="cc-entry-desc">See the command classes that are blocked by default and why each one is dangerous.</span>
      </a>
      <a className="cc-entry-card" href="/guides/how-it-works">
        <span className="cc-entry-label">Guides</span>
        <span className="cc-entry-title">Understand analysis flow</span>
        <span className="cc-entry-desc">Learn how semantic command analysis, wrapper detection, and hook lifecycle checks work.</span>
      </a>
      <a className="cc-entry-card" href="/security">
        <span className="cc-entry-label">Project</span>
        <span className="cc-entry-title">Review security posture</span>
        <span className="cc-entry-desc">Report issues, understand boundaries, and contribute safely.</span>
      </a>
    </div>
  </section>
</div>
