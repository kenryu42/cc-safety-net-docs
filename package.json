{
  "name": "cc-safety-net-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation site for CC Safety Net, built on Mintlify.",
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "preview": "mint dev",
    "validate": "mint validate",
    "broken-links": "mint broken-links --check-anchors --check-redirects --check-snippets",
    "a11y": "mint a11y",
    "check": "bun run validate && bun run broken-links && bun run a11y"
  },
  "devDependencies": {
    "mint": "4.2.598"
  }
}
