---
title: "What CC Safety Net does"
sidebarTitle: "Introduction"
description: "CC Safety Net is a pre-execution guard that inspects what your coding agent is about to run and blocks the operations that destroy uncommitted work or expose your secrets."
---

A single command such as `rm -rf ~/` or `git checkout -- .` can erase hours of uncommitted work. A [documented Reddit incident](https://www.reddit.com/r/ClaudeAI/comments/1pgxckk/claude_cli_deleted_my_entire_home_directory_wiped/) showed Claude CLI deleting an entire home directory.

Instructions in `CLAUDE.md` or `AGENTS.md` can help, but an agent can misinterpret or overlook them. CC Safety Net adds an enforced pre-execution check.

## What it intercepts

CC Safety Net installs into your coding agent. It runs before the agent's tool call reaches your machine.

It inspects shell commands and file write, edit, search, and patch operations. It then allows the operation or blocks it with a direct reason. A block arrives as a normal tool result, so the agent can [continue the task without the blocked operation](/guides/design-principles#denials-that-keep-the-agent-on-task).

The decision is based on intent, not on spelling. `git checkout -b feature` creates a branch and is allowed; `git checkout -- file` discards uncommitted changes and is blocked. Both start with the same two words.

The same inspection protects supported credential-bearing files, including SSH keys, `.env` files, cloud credential stores, and coding CLI tokens. CC Safety Net blocks matching reads and writes before the agent accesses the file. See the [Secret protection reference](/reference/secret-protection) for the full rule set.

Each agent uses a different integration. Some run CC Safety Net as a short-lived subprocess. Others load it in the agent process. The installer configures the correct integration for your agent. For the full lifecycle of one tool call, see [How it works](/guides/how-it-works).

## What it does not replace

CC Safety Net complements your agent's permission rules and native sandboxing. It does not replace them.

Deny rules give you quick, user-configurable blocks. Sandboxing gives operating-system-level filesystem and network containment. CC Safety Net covers known-destructive Git and filesystem operations that a sandbox can permit inside your project. Use these layers together. See [CC Safety Net vs. sandboxing](/guides/vs-sandboxing).

CC Safety Net does not give complete protection. It is a static pre-execution policy gate. It cannot see inside arbitrary binaries, and it cannot protect commands that bypass an installed integration. Read [Known limitations](/guides/known-limitations) before you rely on it.

## Get started

Pick your agent on the [Installation](/installation) page, then confirm protection is live with the [Quickstart](/quickstart).
